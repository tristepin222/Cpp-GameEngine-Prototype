#pragma once

#include <string>

/**
 * @struct Name
 * @brief Component that assigns a user-friendly name to an entity.
 */
struct Name {
    /** @brief The string value of the name. */
    std::string value;
};
